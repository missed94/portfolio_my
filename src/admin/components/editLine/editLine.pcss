.edit-line-component {
  &.blocked {
    opacity: 0.5;
    filter: grayscale(100%);
    pointer-events: none;
  }
}

.title {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.icons {
  display: flex;
  align-items: center;
}

.icon {
  margin-right: 20px;

  &:last-child {
    margin-right: 0;
  }
}

.input-wrapper {
  width: 70%;
}

.buttons {
  display: flex;
}

.button-icon {
  margin-right: 20px;
  

  &:last-child {
    margin-right: 0;
  }
}
